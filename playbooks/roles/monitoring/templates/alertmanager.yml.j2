global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: '{{ alertmanager_receiver }}'

receivers:
  - name: '{{ alertmanager_receiver }}'
    webhook_configs:
      - url: '{{ webhook_url }}'

